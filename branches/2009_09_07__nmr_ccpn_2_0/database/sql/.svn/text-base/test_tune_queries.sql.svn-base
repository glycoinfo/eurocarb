

SELECT DISTINCT(R1.COMP_ID) FROM nmr.tune_shift2atoms R1,
                       nmr.tune_shift2atoms R2
WHERE R1.COMP_ID = R2.COMP_ID
AND R1.SHIFT_FREQ > 4.0
AND R1.SHIFT_FREQ < 4.2
AND R2.SHIFT_FREQ > 4.9
AND R2.SHIFT_FREQ < 5.0;

